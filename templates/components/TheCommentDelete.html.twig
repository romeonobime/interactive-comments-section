
<div {{ attributes }}>
    {% if isOpen %}
        <article class="flex flex-col gap-4 rounded-lg bg-white p-4">
            <h1>Delete comment</h1>
            <p class="body-medium text-neutral-2">Are you sure you want to delete this comment? This will remove the comment and can’t be undone.</p>
            <div>
                <button
                    class="button button-neutral"
                    data-action="live#emit"
                    data-live-event-param="closeModal"
                >Cancel</button>
                <button
                    class="button button-danger"
                    data-action="live#emit"
                    data-live-event-param="commentDeleted"
                    data-live-id-param="{{ id }}"
                >
                    Delete
                </button>
            </div>
        </artilce>
    {% endif %}
</div>
