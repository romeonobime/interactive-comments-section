{% props webp = "", png = "", alt = "" %}

<picture>
	{% if webp != "" %}
		<source srcset="{{ asset(webp) }}" />
	{% endif %}
	{% if png != "" %}
		<source srcset="{{ asset(png) }}" />
	{% endif %}
	<img src="{{ asset(webp) }}" alt="{{ alt }}"/>
</picture>
