{% props id = 0 %}

<button
    class="flex gap-2 items-center fill-danger text-danger hover:fill-danger-light hover:text-danger-light"
        data-action="live#emit"
        data-live-event-param="commentDelete"
        data-live-id-param="{{ id }}"
    >
    <twig:BaseIconDelete />
    <span class="body-medium-bold">Delete</span>
</button>